---
# tasks/05_finish.yml — 部署完成提示

- name: 部署完成
  debug:
    msg:
      - "========================================"
      - "✅ PMDB 部署完成！"
      - "========================================"
      - "目标目录 : {{ deploy_dir }}"
      - "翻译提供商: {{ translate_provider }}"
      - ""
      - "运行程序："
      - "  cd {{ deploy_dir }}"
      - "  ./run.sh"
      - "========================================"
